import {IconsNameType, colorsType} from '@uoslife/design-system';

export type MainServiceBoxType = {
  label: string;
  iconName: IconsNameType;
  iconColor: colorsType;
  children: React.ReactNode;
};

export default MainServiceBoxType;
